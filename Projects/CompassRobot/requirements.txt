# GoPiGo3 Compass Robot Requirements
# Python packages required for IMU-based compass navigation robot

# Option 1: If reusing an existing GoPiGo3 virtual environment
# (mr-gopigo3 is already installed, so only install additional packages):
#   source /path/to/gopigo3/venv/bin/activate
#   pip install DI-Sensors curtsies numpy

# Option 2: If creating a new virtual environment for this project:
#   python3 -m venv venv
#   source venv/bin/activate
#   pip install -r requirements.txt

# Core GoPiGo3 library
mr-gopigo3

# Dexter Industries sensor library (for IMU sensor)
DI-Sensors

# Terminal UI library for keyboard input
curtsies

# Numerical computing library
numpy

# How to run:
#   python compass_robot.py
# Use keyboard controls to navigate with compass heading
