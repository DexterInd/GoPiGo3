# GoPiGo3 Object Avoidance Robot Requirements
# Python packages required for basic obstacle avoidance

# Option 1: If reusing an existing GoPiGo3 virtual environment
# (mr-gopigo3 is already installed, no additional packages needed):
#   source /path/to/gopigo3/venv/bin/activate
#   python object_avoidance_robot.py

# Option 2: If creating a new virtual environment for this project:
#   python3 -m venv venv
#   source venv/bin/activate
#   pip install -r requirements.txt

# Core GoPiGo3 library
mr-gopigo3

# How to run:
#   python object_avoidance_robot.py
# The robot automatically navigates and avoids obstacles using the distance sensor
# Continuously drives forward and turns away when detecting obstacles
