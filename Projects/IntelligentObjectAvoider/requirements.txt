# GoPiGo3 Intelligent Object Avoider Requirements
# Python packages required for ML-based obstacle avoidance

# Option 1: If reusing an existing GoPiGo3 virtual environment
# (mr-gopigo3 is already installed, so only install additional packages):
#   source /path/to/gopigo3/venv/bin/activate
#   pip install scikit-learn

# Option 2: If creating a new virtual environment for this project:
#   python3 -m venv venv
#   source venv/bin/activate
#   pip install -r requirements.txt

# Core GoPiGo3 library
mr-gopigo3

# Machine learning library (for K-means clustering)
scikit-learn

# How to run:
#   python robot.py
# This robot uses K-means clustering to intelligently avoid obstacles
# Uses distance sensor for obstacle detection
